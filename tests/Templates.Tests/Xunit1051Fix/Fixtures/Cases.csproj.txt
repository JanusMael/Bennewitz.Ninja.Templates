<Project Sdk="Microsoft.NET.Sdk">
  <!-- Built in a temp directory, outside this repository's central package management, so the
       xunit.v3 version is substituted from Directory.Packages.props by the test. Warnings are errors,
       as in the suites this script is run on: the script must still build them. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <OutputType>Exe</OutputType>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="xunit.v3" Version="{XUNIT_VERSION}" />
  </ItemGroup>
</Project>

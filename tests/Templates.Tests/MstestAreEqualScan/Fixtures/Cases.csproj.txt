<Project Sdk="Microsoft.NET.Sdk">
  <!-- Built in a temp directory, outside this repository's central package management, so the
       MSTest version is pinned here. MSTest 4 is what scripts/mstest-to-xunit.cs converts from.

       ⚠ TreatWarningsAsErrors is on because real suites run that way and the scan must still build
       them. MSTest's own analyzers are silenced, because they flag these very cases: MSTEST0065
       (AreEqual on an IEnumerable; measured on 4.3.3, it catches the statically-typed collection
       cases but not the opaque one) and MSTEST0032 (an assertion that is always true). A suite built
       under warnings-as-errors with either firing would not compile at all. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <NoWarn>$(NoWarn);MSTEST0065;MSTEST0032</NoWarn>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="MSTest.TestFramework" Version="4.3.3" />
  </ItemGroup>
</Project>
